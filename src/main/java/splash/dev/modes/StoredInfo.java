package splash.dev.modes;

import java.awt.*;

public class StoredInfo {
    public static Color brushColor = Color.WHITE;
    public static PencilMode.BrushMode brushMode = PencilMode.BrushMode.Default;
}